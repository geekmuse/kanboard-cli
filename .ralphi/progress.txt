## 2026-03-23 - US-004
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Added 5 dependency query methods to existing `src/kanboard/resources/portfolios.py`:
  - `get_portfolio_dependencies(portfolio_id, **kwargs)` → `list[dict]`; returns [] on False/None; supports `cross_project_only` kwarg
  - `get_blocked_tasks(portfolio_id)` → `list[dict]`; returns [] on False/None
  - `get_blocking_tasks(portfolio_id)` → `list[dict]`; returns [] on False/None
  - `get_portfolio_critical_path(portfolio_id)` → `list[dict]`; returns [] on False/None
  - `get_portfolio_dependency_graph(portfolio_id, **kwargs)` → `dict`; returns {} on False/None; supports `cross_project_only` kwarg
- Added 25 new unit tests in `tests/unit/resources/test_portfolios.py` (total grows from 48 to 73)
- Methods placed in a new `# Dependency queries` section between task queries and overview sections
- Files changed: `src/kanboard/resources/portfolios.py`, `tests/unit/resources/test_portfolios.py`
- **Learnings for future iterations:**
  - `ruff format` reformats even valid files — always run `ruff format .` after `ruff check .`
  - All 5 methods follow existing list/dict patterns (`if not result: return []` for lists, `result or {}` for dicts)
  - The dict-returning dependency graph method uses same `result or {}` fallback as `get_portfolio_task_count` and `get_portfolio_overview`
---

## 2026-03-23 - US-005
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Extended `KanboardConfig` dataclass in `src/kanboard/config.py` with `portfolio_backend: str = "local"` field
- Added `_ENV_PORTFOLIO_BACKEND = "KANBOARD_PORTFOLIO_BACKEND"` and `_VALID_PORTFOLIO_BACKENDS = frozenset({"local", "remote"})` constants
- Added `cli_portfolio_backend: str | None = None` parameter to `KanboardConfig.resolve()`
- Implemented 4-layer resolution: CLI > `KANBOARD_PORTFOLIO_BACKEND` env var > TOML `portfolio_backend` profile key > `"local"` default
- Added validation: raises `KanboardConfigError(field="portfolio_backend")` with actionable message when value is not 'local' or 'remote'
- Updated docstrings in `KanboardConfig` class and `resolve()` method
- Added 12 new unit tests in `tests/unit/test_config.py` covering all layers, both valid values, 3 invalid paths, and error message content
- Files changed: `src/kanboard/config.py`, `tests/unit/test_config.py`
- **Learnings for future iterations:**
  - The `resolve()` signature now has `cli_portfolio_backend` as a keyword-only param after `config_file` — CLI commands wiring US-007 should pass it as a keyword argument
  - Validation occurs **after** auth field validation (url → token/user-fields → portfolio_backend); invalid backend raises even when auth is valid
  - `_VALID_PORTFOLIO_BACKENDS` is a module-level `frozenset` — a clean pattern for future enum-like string validations
---

## Codebase Patterns
- **PortfoliosResource pattern**: Plugin API resources follow the same pattern as core resources (TYPE_CHECKING guard, _client.call(), typed returns). dict-returning methods (get_portfolio_projects, get_portfolio_tasks, get_portfolio_task_count, get_portfolio_overview) do NOT use PluginPortfolio.from_api() — they return raw dicts or {}. Use `result or {}` for dict fallback (not `if not result: return []`).
- **get_portfolio_by_name raises on False**: Unlike typical list methods that return [] on False, name-lookup methods raise KanboardNotFoundError on both None AND False (per AC).

- **Packaging fixed (fix commit)**: `pyproject.toml` previously listed `kanboard-sdk>=1.1.1` as a runtime dep and only packaged `src/kanboard_cli` in the wheel. This caused `pip install -e '.[dev]'` to reinstall `kanboard-sdk` from PyPI, shadowing `src/kanboard/`. Fixed by: (1) adding `src/kanboard` to `[tool.hatch.build.targets.wheel] packages`, and (2) removing `kanboard-sdk` from dependencies (this repo IS the SDK). `tests/conftest.py` also adds `src/` to the front of `sys.path` as a belt-and-suspenders guard.
- **Model pattern**: All plugin API models go in `src/kanboard/models.py` in the `# Plugin models (Phase 1)` section. Use `_int()`, `_float()`, `_parse_date()` helpers. Export via `__init__.py` imports + `__all__` (sorted, RUF022 requires sorted `__all__`).
- **RUF022**: `__all__` must be sorted lexicographically. "Plugin..." < "Portfolio..." < "Project..." because 'l' < 'o'.
- **Ruff RUF002**: Use ASCII hyphen-minus `-` in docstrings for ranges (0.0-100.0), not en-dash `–`.
---

## 2026-03-24 - US-003
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/kanboard/resources/milestones.py` with all 10 plugin API methods
- Methods: create_milestone, get_milestone, get_portfolio_milestones, update_milestone, remove_milestone, add_task_to_milestone, remove_task_from_milestone, get_milestone_tasks, get_task_milestones, get_milestone_progress
- Wired `MilestonesResource` into `KanboardClient` as `self.milestones`
- Exported `MilestonesResource` from `src/kanboard/__init__.py` (sorted after MilestoneProgress in __all__)
- Added 39 unit tests in `tests/unit/resources/test_milestones.py` covering success, API error, not-found, empty/None/False for every method
- `get_milestone_progress()` raises `KanboardNotFoundError` with `resource="PluginMilestoneProgress"` on None
- Files changed: `src/kanboard/resources/milestones.py`, `src/kanboard/client.py`, `src/kanboard/__init__.py`, `tests/unit/resources/test_milestones.py`
- **Learnings for future iterations:**
  - `ruff format` may reformat new files even when `ruff check` passes — always run format after check (same gotcha as US-002)
  - dict-returning task/progress methods follow the same `result or {}` / `if not result: return []` patterns as PortfoliosResource
  - `add_task_to_milestone` raises KanboardAPIError on False (same as add_project_to_portfolio); remove_task_from_milestone just returns bool (no raise)
---

## PRD Run Context
PRD Branch: ralph/phase-1-dual-backend-orchestration
Started: 2026-03-24T02:50:05.636Z
---

## 2026-03-24 - US-001
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Added 3 new plugin dataclass models to `src/kanboard/models.py`:
  - `PluginPortfolio` (id, name, description, owner_id, is_active, created_at, updated_at)
  - `PluginMilestone` (id, portfolio_id, name, description, target_date, status, color_id, owner_id, created_at, updated_at)
  - `PluginMilestoneProgress` (milestone_id, total, completed, percent, is_at_risk, is_overdue)
- All 3 models have `from_api()` classmethods with proper `_int()`, `_parse_date()`, `_float()`/`float()`, `bool()` coercions
- Exported from `src/kanboard/__init__.py` in sorted order per RUF022
- Added 36 new unit tests in `tests/unit/test_models.py` covering happy path, missing fields, type coercions, edge cases
- Files changed: `src/kanboard/models.py`, `src/kanboard/__init__.py`, `tests/unit/test_models.py`
- **Learnings for future iterations:**
  - The pre-commit hook runs `pip install -e '.[dev]'` which re-installs `kanboard-sdk` 1.1.1 from PyPI, putting a stale `kanboard/` directory back in site-packages after every commit. Must run `pip uninstall kanboard-sdk` before the next test run.
  - RUF002: use ASCII `-` not `–` (en-dash) in docstring range examples
  - `PluginMilestone.from_api()` uses `str(data.get("color_id", ""))` — if the API returns `None`, this yields `"None"` (the string). Consider guarding: `str(data.get("color_id") or "")` in future if needed.
---

## 2026-03-24 - US-002
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Implemented `src/kanboard/resources/portfolios.py` with all 13 plugin API methods
- Methods: create_portfolio, get_portfolio, get_portfolio_by_name, get_all_portfolios, update_portfolio, remove_portfolio, add_project_to_portfolio, remove_project_from_portfolio, get_portfolio_projects, get_project_portfolios, get_portfolio_tasks, get_portfolio_task_count, get_portfolio_overview
- Wired `PortfoliosResource` into `KanboardClient` as `self.portfolios`
- Exported `PortfoliosResource` from `src/kanboard/__init__.py` (sorted in __all__ between PortfolioManager and Project)
- Added 48 unit tests in `tests/unit/resources/test_portfolios.py` covering success, API error, not-found, empty/None/False for every method
- Files changed: `src/kanboard/resources/portfolios.py`, `src/kanboard/client.py`, `src/kanboard/__init__.py`, `tests/unit/resources/test_portfolios.py`
- **Learnings for future iterations:**
  - dict-returning methods use `result or {}` for the empty fallback (not `if not result: return []`)
  - get_portfolio_by_name() raises KanboardNotFoundError on both None and False (unlike list methods which return [])
  - `ruff format` may reformat new files even when `ruff check` passes — always run format after check
---

## 2026-03-24 - US-006
Session: N/A
Review Outcome: PASS
Trajectory: ON_TRACK
Trajectory Notes: N/A
Corrective Plan: N/A
- Created `src/kanboard/orchestration/backend.py` with:
  - `PortfolioBackend` — `@runtime_checkable` Protocol defining 12 method signatures matching `LocalPortfolioStore`'s public API (load, create_portfolio, get_portfolio, update_portfolio, remove_portfolio, add_project, remove_project, add_milestone, update_milestone, remove_milestone, add_task_to_milestone, remove_task_from_milestone)
  - `RemotePortfolioBackend` — adapter wrapping `PortfoliosResource` + `MilestonesResource`; translates name-based interface to ID-based plugin API calls via `_resolve_portfolio_id()` and `_resolve_milestone_id()` helpers
  - `_build_portfolio()` — fetches project_ids via `get_portfolio_projects()` and milestones via `get_portfolio_milestones()` to build full `Portfolio` model from `PluginPortfolio`
  - `_to_milestone()` — fetches task_ids via `get_milestone_tasks()` to build `Milestone` model from `PluginMilestone`
  - `create_backend()` factory — returns `LocalPortfolioStore` for 'local', `RemotePortfolioBackend` for 'remote', raises `KanboardConfigError` for missing client or invalid type
- Updated `src/kanboard/orchestration/__init__.py` to export `PortfolioBackend`, `RemotePortfolioBackend`, `create_backend`
- Updated `src/kanboard/__init__.py` to export all three new symbols (sorted per RUF022)
- Added 50 unit tests in `tests/unit/orchestration/test_backend.py` — 100% coverage on backend.py
- Files changed: `src/kanboard/orchestration/backend.py` (new), `src/kanboard/orchestration/__init__.py`, `src/kanboard/__init__.py`, `tests/unit/orchestration/test_backend.py` (new)
- **Learnings for future iterations:**
  - `PortfolioBackend` Protocol uses `load()` (not `list_portfolios`) — matching LocalPortfolioStore's actual method name so it satisfies the protocol without modification
  - `@runtime_checkable` Protocol enables `isinstance(store, PortfolioBackend)` checks in tests
  - `RemotePortfolioBackend` is O(2N+1) API calls for `load()` (1 getAllPortfolios + N getPortfolioProjects + N getPortfolioMilestones); acceptable for typical portfolio counts
  - The `critical` flag in `add_task_to_milestone` is protocol-compatible but ignored server-side (plugin API doesn't support it natively)
  - `_resolve_milestone_id()` raises `KanboardConfigError` (not `KanboardNotFoundError`) for consistency with LocalPortfolioStore's milestone-not-found exception type
  - `remove_portfolio` and `remove_milestone` catch lookup exceptions and return `False` instead of propagating — matching LocalPortfolioStore's non-raising behavior on not-found for remove operations
---
